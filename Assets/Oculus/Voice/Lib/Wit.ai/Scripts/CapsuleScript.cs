using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CapsuleScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void updateCapsuleMaterial(int forceFromArduino) {
        //if force from arudino is > 1.
        // change material color of capsule.

        //add to serial comm script
    }
}
